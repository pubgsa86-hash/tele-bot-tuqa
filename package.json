{
  "name": "media-converter-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "web:dev": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "start": "python -m bot.main"
  }
}